import Link from 'next/link'; import type { Locale } from '@/lib/site/locale'; import { visibleNavigation } from '@/lib/site/navigation'; import styles from './SiteShell.module.css';
export function DesktopNavigation({ locale }: { locale: Locale }) { return <nav className={styles.desktopNav} aria-label={locale === 'fr' ? 'Navigation principale' : 'Main navigation'}>{visibleNavigation(locale).map((group) => <div className={styles.dropdown} key={group.id}><button type="button" aria-haspopup="true">{group.label[locale]}<span aria-hidden="true">⌄</span></button><div className={styles.megaMenu}>{group.items.map((entry) => <Link key={entry.id} href={entry.href!}>{entry.label[locale]}</Link>)}</div></div>)}<Link className={styles.directLink} href={locale === 'fr' ? '/pricing' : '/pricing?lang=en'}>{locale === 'fr' ? 'Tarification' : 'Pricing'}</Link></nav>; }
