import Link from 'next/link'; import type { Locale } from '@/lib/site/locale'; import { switchLocaleHref } from '@/lib/site/locale'; import styles from './SiteShell.module.css';
export function LanguageSwitcher({ locale, pathname }: { locale: Locale; pathname: string }) { const nextLocale = locale === 'fr' ? 'en' : 'fr'; return <Link className={styles.language} href={switchLocaleHref(pathname, locale)} hrefLang={nextLocale === 'fr' ? 'fr-CA' : 'en-CA'} aria-label={locale === 'fr' ? 'View this page in English' : 'Voir cette page en français'}>{nextLocale.toUpperCase()}</Link>; }
